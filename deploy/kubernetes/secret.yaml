apiVersion: v1
kind: Secret
metadata:
  name: krx-rule-mcp-secret
type: Opaque
stringData:
  # The server rejects this placeholder; replace it before deployment.
  RULE_MCP_BEARER_TOKEN: "REPLACE_WITH_STRONG_RANDOM_TOKEN"
