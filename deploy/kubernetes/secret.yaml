apiVersion: v1
kind: Secret
metadata:
  name: krx-rule-mcp-secret
type: Opaque
stringData:
  # The server rejects this placeholder registry; replace it before deployment.
  bearer-tokens.yaml: |
    version: 1
    tokens:
      - id: replace-me
        sha256: REPLACE_WITH_SHA256_OF_BEARER_TOKEN
        enabled: true
